import { Typography } from '@mui/material';

const Home = () => {
  return (
    <Typography pt={5} variant="h3" textAlign="center">
      Welcome to page!
    </Typography>
  );
};

export default Home;
